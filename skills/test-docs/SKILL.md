---
name: test-docs
description: 用于生成或更新 docs/test 下的测试文档，尤其是测试策略、用例矩阵、联调验证、回归范围、数据准备或开发评审前需要测试报告时触发。
---

# Test Docs

## 输出位置

- 文档路径：`docs/test/<业务域>.md`
- 索引路径：`docs/test/index.md`
- 地图路径：`docs/map.md`

## 写作规则

- 先读取 `docs/map.md`、`docs/test/index.md`、相关 PRD、API 协议、架构文档和已有测试文档。
- 测试文档必须服务开发评审；实现前需要评审时，只输出测试设计，不直接生成测试代码。
- 用例必须覆盖正常路径、边界条件、权限/状态、错误路径和回归范围。
- 新增或修改测试策略、测试分层、Mock/fixture 策略、E2E/联调范围、覆盖率边界、权限/状态机/数据一致性验证时，属于 L2，必须先输出《测试设计报告》并等待开发者确认。
- 已有明确 PRD/API/缺陷复现步骤，且只补充既有模式下的单元、组件或回归用例时，可按 L0 直接生成测试代码，并在交付中说明跳过评审依据。
- 更新或新增文档后，同步更新 `docs/test/index.md` 和 `docs/map.md`；`docs/map.md` 必须维护业务域到 PRD/API/组件/测试文档的导航关系。

## 文档结构

```md
# <业务域>测试文档

## 测试目标

## 范围

## 测试分层

## 用例矩阵

| 场景 | 前置条件 | 操作 | 预期 | 层级 | 状态 |
|---|---|---|---|---|---|

## 测试数据

## Mock 与依赖

## 回归范围

## 风险与待确认
```

## 示例

```md
# 采购订单测试文档

## 测试目标

验证采购订单列表筛选、详情展示、审批状态和关闭限制符合 PRD 与接口契约。

## 测试分层

- 单元测试：筛选参数构造、状态展示映射。
- 组件测试：列表筛选表单、空态、错误态。
- 接口联调：`GET /api/purchase-orders` 查询参数与响应渲染。

## 用例矩阵

| 场景 | 前置条件 | 操作 | 预期 | 层级 | 状态 |
|---|---|---|---|---|---|
| 按供应商筛选 | 存在供应商 A/B 的订单 | 输入供应商 A 并查询 | 只展示供应商 A 的订单 | 组件/联调 | planned |
| 关闭订单限制 | 订单状态为 closed | 点击发起审批 | 操作不可用并展示原因 | 组件 | planned |

## 风险与待确认

- MISSING：后端是否返回关闭状态下的可操作动作列表。
```
