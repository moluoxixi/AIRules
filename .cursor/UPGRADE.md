# Moluoxixi Skills Upgrade Guide (Cursor)

## Command

### macOS / Linux

```bash
git -C ~/.moluoxixi pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host cursor --mode upgrade
```

### Windows PowerShell

```powershell
git -C "~/.moluoxixi" pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host cursor --mode upgrade
```
