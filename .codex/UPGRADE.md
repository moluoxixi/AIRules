# Moluoxixi Skills Upgrade Guide (Codex)

## Command

### macOS / Linux

```bash
git -C ~/.moluoxixi pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host codex --mode upgrade
```

### Windows PowerShell

```powershell
git -C "~/.moluoxixi" pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host codex --mode upgrade
```
