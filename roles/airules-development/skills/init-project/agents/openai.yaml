interface:
  display_name: 'AIRules Project Init'
  short_description: 'Install the AIRules development workflow runtime'
  default_prompt: 'Use $init-project to initialize the AIRules development workflow in this repository.'
