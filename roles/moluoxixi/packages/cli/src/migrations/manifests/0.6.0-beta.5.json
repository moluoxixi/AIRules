{
  "version": "0.6.0-beta.5",
  "description": "Beta patch: cherry-picks v0.5.10 stable fixes (git-add-f prevention + Pi #246/#249) + version-update hint at session start (PR #254).",
  "breaking": false,
  "recommendMigrate": false,
  "changelog": "**Bug Fixes:**\n- fix(scripts): `add_session.py` and `task.py archive` no longer print a generic `git add .moluoxixi && git commit` fallback when the repo's `.gitignore` excludes `.moluoxixi/`. They now stage only specific Moluoxixi-owned paths and auto-retry with `git add -f -- <specific-paths>` only on `ignored by` stderr. The warning text explicitly states `Do NOT use \\`git add -f .moluoxixi/\\``, naming `.moluoxixi/.backup-*`, `.moluoxixi/worktrees/`, `.moluoxixi/.template-hashes.json`, `.moluoxixi/.runtime/`, `.moluoxixi/.cache/` as the subpaths users should ignore individually instead. Helper centralized in `templates/moluoxixi/scripts/common/safe_commit.py`.\n- fix(pi): Pi extension now injects `<workflow-state>` breadcrumb on every `input` and `before_agent_start` event, plus a `<session-overview>` block sourced from `.moluoxixi/scripts/get_context.py`. The `subagent` tool registration carries a `promptSnippet` with the `Active task: <path>` dispatch protocol. Closes #249.\n- fix(pi): Project-level `packages[\"npm:pi-subagents\"]` override added to `.pi/settings.json` so a globally-installed `npm:pi-subagents` cannot inject `extensions / skills / prompts / themes` into the current Moluoxixi project. `scrubPiSettings` reverses the override on `moluoxixi uninstall`. Closes #246.\n\n**Enhancements:**\n- feat(scripts): `get_context.py` default mode now performs a once-per-session `moluoxixi --version` check and prepends `Moluoxixi update available: <current> -> <latest>, run npm install -g @moluoxixi/airules-moluoxixi-cli@latest` before the context body when the local install lags. Best-effort with a 1-second timeout; failures silently skip. Marker stored under `.moluoxixi/.runtime/` (auto-ignored). Closes #254.",
  "migrations": [],
  "notes": "Beta patch on top of 0.6.0-beta.4. Run `moluoxixi update`. Brings the four 0.5.10-line fixes / features into the 0.6 beta line."
}
