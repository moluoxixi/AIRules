{
  "version": "0.6.0-beta.23",
  "description": "Beta patch: dispatch channel runtime agent definitions, refresh registry-backed spec via update, add Reasonix platform support, ship moluoxixi-session-insight bundled skill, and rename moluoxixi-spec-bootstarp → moluoxixi-spec-bootstrap.",
  "breaking": false,
  "recommendMigrate": true,
  "changelog": "**Enhancements:**\n- feat(cli): `trellis init` and `trellis update` now ship `.trellis/agents/{check,implement}.md`, so switching to a channel-driven workflow no longer fails with \"Agent 'check' not found\" (#323).\n- feat(cli): `trellis update` refreshes registry-backed `.trellis/spec` templates through the existing hash/conflict flow. `trellis init --template <id>` persists the spec source/template in `.trellis/config.yaml` so subsequent updates can re-fetch it. Supports direct spec registries and marketplace-style registries (#324).\n- feat(platform): add **Reasonix** (DeepSeek-Reasonix) as the 15th supported AI coding tool via `trellis init --reasonix`. Subagent skills (`moluoxixi-implement`, `moluoxixi-check`) carry `runAs: subagent` frontmatter so Reasonix spawns them as isolated subagent loops (#301).\n- feat(skills): bundle `moluoxixi-session-insight` skill that teaches the AI how and when to reach for `trellis mem` to recall past conversations across Claude Code and Codex sessions. Auto-dispatched to every supported platform on init/update.\n- feat(workflow): `trellis workflow --template <id>` and `trellis init --workflow <id>` print a stderr warning when the resolved workflow references `.trellis/agents/<name>.md` files that are missing on disk, pointing the user at `trellis update`.\n\n**Bug Fixes:**\n- fix(skills): rename bundled `moluoxixi-spec-bootstarp` → `moluoxixi-spec-bootstrap` (typo). `trellis update --migrate` ships a `rename-dir` migration that renames the installed directory across 13 platform skill roots; missing roots are silently skipped. Historical migration manifests `0.5.17.json` and `0.6.0-beta.18.json` keep the typoed text because they describe what already shipped to users on those versions (#296).",
  "migrations": [
    {
      "type": "rename-dir",
      "from": ".claude/skills/moluoxixi-spec-bootstarp",
      "to": ".claude/skills/moluoxixi-spec-bootstrap",
      "description": "Claude Code: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".cursor/skills/moluoxixi-spec-bootstarp",
      "to": ".cursor/skills/moluoxixi-spec-bootstrap",
      "description": "Cursor: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".opencode/skills/moluoxixi-spec-bootstarp",
      "to": ".opencode/skills/moluoxixi-spec-bootstrap",
      "description": "OpenCode: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".agents/skills/moluoxixi-spec-bootstarp",
      "to": ".agents/skills/moluoxixi-spec-bootstrap",
      "description": "Codex / Gemini CLI (shared): rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".kiro/skills/moluoxixi-spec-bootstarp",
      "to": ".kiro/skills/moluoxixi-spec-bootstrap",
      "description": "Kiro: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".qoder/skills/moluoxixi-spec-bootstarp",
      "to": ".qoder/skills/moluoxixi-spec-bootstrap",
      "description": "Qoder: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".codebuddy/skills/moluoxixi-spec-bootstarp",
      "to": ".codebuddy/skills/moluoxixi-spec-bootstrap",
      "description": "CodeBuddy: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".github/skills/moluoxixi-spec-bootstarp",
      "to": ".github/skills/moluoxixi-spec-bootstrap",
      "description": "GitHub Copilot: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".factory/skills/moluoxixi-spec-bootstarp",
      "to": ".factory/skills/moluoxixi-spec-bootstrap",
      "description": "Droid: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".pi/skills/moluoxixi-spec-bootstarp",
      "to": ".pi/skills/moluoxixi-spec-bootstrap",
      "description": "Pi Agent: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".agent/skills/moluoxixi-spec-bootstarp",
      "to": ".agent/skills/moluoxixi-spec-bootstrap",
      "description": "Antigravity: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".windsurf/skills/moluoxixi-spec-bootstarp",
      "to": ".windsurf/skills/moluoxixi-spec-bootstrap",
      "description": "Windsurf: rename installed bundled skill directory from typoed name to corrected name."
    },
    {
      "type": "rename-dir",
      "from": ".kilocode/skills/moluoxixi-spec-bootstarp",
      "to": ".kilocode/skills/moluoxixi-spec-bootstrap",
      "description": "Kilo: rename installed bundled skill directory from typoed name to corrected name."
    }
  ],
  "notes": "Beta patch on top of 0.6.0-beta.22. Run `trellis update --migrate` to rename installed `moluoxixi-spec-bootstarp/` skill directories to `moluoxixi-spec-bootstrap/` across every platform you have configured (silently skips ones you do not). Plain `trellis update` also installs the new `moluoxixi-session-insight` bundled skill and the `.trellis/agents/{check,implement}.md` channel runtime files."
}
