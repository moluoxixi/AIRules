# Moluoxixi Skills Upgrade Guide (Qoder)

## Command

### macOS / Linux

```bash
git -C ~/.moluoxixi pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host qoder --mode upgrade
```

### Windows PowerShell

```powershell
git -C "~/.moluoxixi" pull --ff-only
npx tsx ~/.moluoxixi/scripts/host-setup.ts --host qoder --mode upgrade
```
