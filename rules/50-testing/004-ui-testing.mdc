---
description: UI 测试规范 - 使用 Playwright MCP 验证效果
globs:
  - "**/*.vue"
  - "**/*.tsx"
  - "**/*.jsx"
---

# UI 测试规范

> **核心要求：涉及 UI 的改动必须使用 Playwright MCP 验证效果**

## 强制要求

| 场景 | 要求 |
|------|------|
| 新建组件 | ✅ 必须用 Playwright 截图验证 |
| 修改组件样式 | ✅ 必须验证改动效果 |
| 修改布局 | ✅ 必须验证页面渲染 |
| 修复 UI Bug | ✅ 必须验证修复效果 |

## MCP 验证原则

- 禁止使用“快速测试”“抽样测试”“随机检查”“只验证主要流程”等不完整表述和做法
- 每次验证都必须围绕当前改动设计完整场景，覆盖正常流程、边界条件、异常路径和关键交互
- 验证步骤必须与页面结构、业务规则、用户操作路径一一对应，不能套用固定模板敷衍执行
- 验证结论必须来自实际 MCP 操作结果、页面表现、截图和控制台信息，不能凭代码推断代替
- 如果场景复杂，必须拆分成多个具体验证用例逐项完成，直到本次改动相关风险被完整覆盖

## 测试流程

```
开发/修改 UI → 梳理本次改动场景与风险点 → 启动本地服务 → 使用 Playwright MCP 按场景逐项操作验证 → 分场景截图与检查控制台 → 确认结果
```

## Playwright MCP 使用

### 1. 导航到页面

```
使用 mcp_playwright_browser_navigate 访问本地开发服务器
URL: http://localhost:5173/your-page
```

### 2. 获取页面快照

```
使用 mcp_playwright_browser_snapshot 获取页面结构
用于验证元素是否正确渲染
```

### 3. 截图验证

```
使用 mcp_playwright_browser_take_screenshot 截图
验证视觉效果是否符合预期
```

### 4. 交互测试

```
使用 mcp_playwright_browser_click 测试点击
使用 mcp_playwright_browser_type 测试输入
使用 mcp_playwright_browser_fill_form 测试表单
```

## 验证检查清单

- [ ] 已根据本次改动整理完整场景清单，而非快速抽样
- [ ] 组件在页面上正确渲染
- [ ] 样式符合设计稿/预期
- [ ] 交互功能正常
- [ ] 边界条件与异常路径已验证
- [ ] 无控制台错误
