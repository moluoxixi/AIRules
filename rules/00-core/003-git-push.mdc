---
description: Git 提交推送规则 - 仅在用户明确要求时才执行
alwaysApply: true
---

# Git 提交推送约束

## 强制要求

| 操作 | 规则 |
|---|---|
| `git commit` | **必须用户明确要求**才能执行 |
| `git push` | **必须用户明确要求**才能执行 |
| 自动提交 | ❌ 绝对禁止 |
| 自动推送 | ❌ 绝对禁止 |

## 允许的场景

```
✅ 用户说："提交代码" / "commit" / "推送" / "push"
✅ 用户说："提交推送代码"
```

## 禁止的场景

```
❌ 任务完成后自动 commit
❌ 修复 bug 后自动 push
❌ AI 认为"应该提交了"就提交
❌ 任何未经用户确认的 git 写操作
```

**原则：git commit / push 的决定权完全在用户手中。**
